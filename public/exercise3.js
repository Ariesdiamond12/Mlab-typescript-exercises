"use strict";
let sum = (num1, num2) => {
    return num1 + num2;
};
console.log(sum(5, 9));
let newSum = (x, y) => {
    if (y) {
        return x.toUpperCase();
    }
    else {
        return x.toLowerCase();
    }
};
console.log();
